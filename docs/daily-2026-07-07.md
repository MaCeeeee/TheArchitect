---
date: 2026-07-07
tags: [daily, the-390, uc-canon-001, norm-facade, adversarial-verification, prod-deploy, migration, the-419, the-435]
status: closed
---

# 2026-07-07 — THE-390 P1–P4a gebaut & gemerged + Prod nach 13 Tagen deployed

## TL;DR

Vom Konzept in die Realisierung: das kanonische Norm-Metamodell (ADR-0004) wurde in **P0 adversarial gehärtet** (6 Red-Team-Runden, 28 Befunde, alle additiv geschlossen → P0-Gate-Entscheidung), dann in **vier mergebaren Scheiben P1→P4a gebaut** (Model+Facade → Pipeline → Requirements/Gap → Writes+Migration). Damit läuft eine gecrawlte Regulation lesend+schreibend durch die Pipeline, und Upload-Standards bekommen Requirements. **THE-419** (Strangler-Cutover-Rest) und **P1–P4a** sind auf `master`. Zum Abschluss **Prod nach ~13 Tagen Rückstand deployt** (golden workflow, bridge-sicher) und die **Migration als verlustfrei verifiziert** (Dry-Runs, 0 skipped) — Apply bewusst nach P5.

## Teil 1 — P0 adversarial gehärtet + Gate-Entscheidung

- 6 Red-Team-Runden gegen das Meta-Modell (`docs/strategy/2026-07-05-canon-architecture-design.md` §10). **28 Befunde**, alle rein additiv geschlossen (neue Ontologie-Beziehungstypen `DEROGATED_BY`/`SETS_PARAMETER`/`RECOGNIZES_EQUIVALENCE`/`INTERPRETS` + optionale Reference-Data-Felder). **Kern-Schema seit Runde 3 unverändert.**
- Echte Fänge: lex specialis (DORA↔NIS2), Extraterritorialität (DSGVO Art. 3(2), blocker), zwei Selbstwidersprüche (geschlossene Enums bei `PartyRole`/`AssuranceScheme`).
- **P0-Gate-Entscheidung:** Exit-Kriterium von „exakt leere Runde" (asymptotisch unerreichbar) auf **„Kern stabil + jede Lücke additiv absorbierbar"** = Beweis der Generalisierungs-These. ADR-0004 auf **Accepted**.

## Teil 2 — Linear angelegt

- **THE-412** UC-CANON-001 (Score 74,3, blockedBy THE-368) + **THE-413–418** (6 REQs).
- **THE-419** (Strangler-Cutover-Rest, aus THE-368-Audit) + **THE-435** (Jest-Worker-Crash-Tech-Debt).

## Teil 3 — THE-419 Strangler-Cutover ([PR #34](https://github.com/MaCeeeee/TheArchitect/pull/34))

- Fallback-Telemetrie (`corpusUnconfigured`/`corpusMiss`) + Kill-Switch `CORPUS_STRICT_READS`; Mapping-Service vom legacy `IRegulation` entkoppelt (`MappingRegulationInput`).
- **Prod-Befund:** Image 10 Tage alt, **60/60 Mappings ohne Korpus-Referenz**, 19 legacy Regs → THE-368-Daten-ACs nie erfüllt. Daten-Migration bewusst nach THE-390 P4 verschoben (gegen Doppel-Migration). **Done.**

## Teil 4 — P1 Model + Facade ([PR #36](https://github.com/MaCeeeee/TheArchitect/pull/36))

- `shared/norm.types.ts` (NormView/NormMappingView + Identität `workId`+Alias), `Norm.ts`/`NormMapping.ts` (Zielschema, in P1 nicht beschrieben), `norm.service.ts` (Lese-Facade über Standard+Regulation, read-only, Strangler). Korpus-Norm = ganzes Gesetz, Status getrennt (conformance/lifecycle), Legacy-Mapping ohne regulationKey ehrlich übersprungen. Koexistiert kollisionsfrei mit der parallel gemergten THE-429-Ontologie.

## Teil 5 — P2 Pipeline auf Facade ([PR #37](https://github.com/MaCeeeee/TheArchitect/pull/37))

- **Ab hier laufen Regulations durch die Pipeline:** `getPipelineNorm` + `computeNormMappingStats` (Korpus projiziert lifecycle→conformance), `CompliancePipelineState.normId` + deterministischer Anchor (`derivePipelineAnchorId`, stirbt in P4). **Kritischer Fund behoben:** Portfolio-Orphan-Cleanup hätte Korpus-States sofort gelöscht. Remediation + AI-Match lesen beide Welten. Adapter `POST /norms/:workId/pipeline`.

## Teil 6 — P3 Requirements/Gap auf Norm ([PR #38](https://github.com/MaCeeeee/TheArchitect/pull/38))

- `ComplianceRequirement.normId`/`sectionEId`; REQGEN persistiert gegen **jede** Norm (Korpus-Gesetze **und** Upload-Standards → „Standards bekommen Requirements" ✓). `/generate` löst Sections server-seitig; Confirm-Endpoint (Human-Gate) normfähig; Gap-Analyse norm-aware im Mischbetrieb, LIVE-Berechnung erhalten.

## Teil 7 — P4a Writes + Migration ([PR #39](https://github.com/MaCeeeee/TheArchitect/pull/39))

- `upsertNormDoc`/`materializeProjectNorms` — die `Norm`-Collection wird geschrieben; Upload materialisiert seine Norm mit (best-effort). `scripts/migrate-to-norms.ts` (dry-run default, idempotent). Read-Facade bleibt vorerst auf den Quell-Collections (Read-Cutover = P4b).

## Teil 8 — Prod-Deploy (Server A, 13 Tage Rückstand)

- **Vorab THE-364 geprüft:** goldener Workflow (`-f docker-compose.prod.yml … --force-recreate app`) rekreiert nur den App-Container → **Bridge bleibt heil**, THE-364-Ausfallpfad nicht ausgelöst. Kein Blocker.
- **1. Versuch war ein Cache-No-Op** — der rsync des neuen Codes fehlte → Build lief gegen alte Quelle (`COPY packages/… CACHED`). Erkannt, korrigiert.
- **2. Versuch echt:** rsync `packages/` + `package-lock.json` (nur die driftete — Pitfall 5), dann `npm ci` frisch (33s), Build neu. **`Server running` + HTTP 200.** Prod aktuell: P1–P4a + Rückstand (THE-397 Email-Guard, THE-419, THE-429, THE-342, Evals).
- **Migrations-Dry-Runs auf Prod (verlustfrei, 0 skipped):**
  - `migrate-mapping-references`: total=60 · updated=60 · skipped=0
  - `migrate-to-norms`: projects=8 · norms=11 (aktuell nur Upload-Standards) · requirements=10/10 · skipped=0
- **Apply bewusst nach P5** (einmal aufs Zielschema). Reihenfolge fix: `migrate-mapping-references --apply` **dann** `migrate-to-norms --apply`. Container-Pfad `/app/packages/server/dist/scripts/`.

## Teil 9 — Nebenprodukte

- **Manueller Abnahme-Testplan:** `docs/test-guide-norm-pipeline.md` (4 Strecken + Abbruchkriterien inkl. Asilomar-Check).
- **ArchiMate-Metamodell-Diagramm** für den EA gezeichnet (Norm→Provision→Pflicht→Kontrolle/Subjekt→Bewertung→Befund→Behebung, 3 Ebenen).
- **THE-435** angelegt: Jest-Worker-Crashes bei parallelen Schwer-Suites (falsches Rot, Vorbestand).

## PRs heute

| PR | Inhalt | Status |
|---|---|---|
| [#34](https://github.com/MaCeeeee/TheArchitect/pull/34) | THE-419 Strangler-Cutover (Telemetrie + Kill-Switch) | ✅ merged |
| [#36](https://github.com/MaCeeeee/TheArchitect/pull/36) | P1 Norm-Model + Facade | ✅ merged |
| [#37](https://github.com/MaCeeeee/TheArchitect/pull/37) | P2 Pipeline auf Facade | ✅ merged |
| [#38](https://github.com/MaCeeeee/TheArchitect/pull/38) | P3 Requirements/Gap auf Norm | ✅ merged |
| [#39](https://github.com/MaCeeeee/TheArchitect/pull/39) | P4a Writes + Migration | ✅ merged |

## Morgen — P4b (frische Session empfohlen)

P4b ist client-lastig (~12+ React-Dateien) → eigene Session mit Kontext-Raum. Inhalt:
1. [ ] Kurzer **P4b-Plan** (wie P1-Plan), User-Abnahme.
2. [ ] Unique-Index-/FK-Flip auf `normId`/`corpusRef` (ADR-0004 E4/E5).
3. [ ] Client-Konsolidierung: `standardsAPI`→`normsAPI`, Norm-Manager (Upload-Tab + Korpus-Browse-Tab), „Add to pipeline"-UI, Matrix/GapAnalysis/PropertyPanel umstellen.
4. [ ] Read-Cutover Facade→Norm-Collection, Alt-Collections stilllegen.
5. Danach **P5**: E2E nach Testplan + die zwei `--apply`-Migrationen + Deploy.

## Offene Fäden (nicht vergessen)

- [ ] **Prod-`.env` hat kein `CORPUS_MONGODB_URI`** — Korpus-Reads laufen dort via App-DB-Fallback. Klären für P4b/P5, ob Prod die Server-B-Korpus-Gesetze überhaupt sieht (sonst nur die 19 legacy Regs).
- [ ] **THE-364** (mongo-tailnet-bridge in App-Compose) — eigenes Wartungsfenster, vor dem *nächsten* Voll-Stack-Recreate.
- [ ] **THE-435** (Jest-Worker-Crashes) — CI-Signal unzuverlässig, wert eigenständig zu fixen.
- [ ] P0-Auflagen aus ADR-0004 E8: Reach/Derogation als echte **Query-Logik**, Status-Vokabular-Vereinigung, 28-Klauseln-Regressionssuite, Facade-`kind`/`jurisdiction` gegen THE-429-Ontologie validieren.
- [ ] Heartbeat: THE-419/368 zu → **THE-412 re-scoren** (Feasibility steigt).

## Verwandt

[[daily-2026-07-05]] · [[ADR-0004]] · [[progress_uc_canon_001]] · `docs/strategy/2026-07-05-canon-architecture-design.md` · `docs/superpowers/plans/2026-07-05-the390-p1-model-facade.md` · `docs/test-guide-norm-pipeline.md` · THE-390 · THE-412 · THE-419 · THE-435
