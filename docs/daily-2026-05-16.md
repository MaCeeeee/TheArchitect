# Daily — 2026-05-16 (Samstag)

## Tagesfokus

Sprint 3 Tag 1 — von Pre-Flight bis Production-Live durchgezogen. **4 Commits, UC-RED-001 Slice 1 deployed**, BSH-pitchbare Demo-Story sofort sichtbar.

```
Slice 1 (REQ-RED-001 + REQ-RED-003) Production-Live
End-to-End auf BSH-ESG-Demo: 20 Redundanz-Kandidaten in 15 von 61 Elementen,
Top-Match Supplier-Master ↔ Supplier-Contact-Information mit 85% (SAME).
```

---

## Geliefert

### Bug-Fix — Token-Refresh in 4 Generator-Hooks (Sprint-2-Carry-Over)
- `authFetch.ts` Wrapper als drop-in für `fetch()` mit Refresh-Token-Logic (in-flight-Promise gegen parallele Refresh-Storms)
- Die 4 Generator-Hooks (useDataObjectGenerator, useActivityGenerator, useProcessGenerator, useHierarchyGenerator) jetzt mit `authFetch` statt direkt `fetch+Bearer`
- 6 Vitest-Cases für den Wrapper (happy path, refresh-success, refresh-failure, generic 401, non-JSON 401)
- Commit `686754e`

### Pre-Flight UC-RED-001 + Linear-Issue-Setup
- 8-Kriterien-Scoring für 5 REQs durchgerechnet:
  - REQ-RED-001 Endpoint = 65.7
  - REQ-RED-002 Cross-Type = 62.9
  - REQ-RED-003 List-UI = 71.4 (höchstes Slice-1-Score)
  - REQ-RED-004 Bulk-Merge = 65.7
  - REQ-RED-005 Audit = 51.4
- Linear-Issues via MCP angelegt: **THE-244** (Parent UC-RED-001) + **THE-245/246/247/249/250** (5 REQs)

### REQ-RED-001 Backend (Slice 1 Backend, commit `0aab456`)
- **Service `findRedundancies`** in `elementSimilarity.service.ts`:
  - Driver-Loop: pro Element `findSimilarElements({elementId, topK, scoreThreshold})`
  - Pair-Dedup via sorted-key Map (A-B == B-A)
  - Same-type-Filter (default) blockt Cross-Layer-Matches
  - **DI-Hook** auf findSimilarElements für Unit-Tests
  - Higher-Score-wins bei asymmetrischen Match-Werten
  - O(n) Qdrant round-trips — performance-tauglich bis ~500 Elements
- **Route** `GET /api/projects/:projectId/redundancies`:
  - Query-params via Zod: `type`, `scoreThreshold`, `topK`, `limit`, `sameTypeOnly`
  - Default-Type-Filter = data-* (data_object/entity/model)
  - Rate-Limit 30/min (gleicher Bucket wie /similar)
  - Audit-action `redundancy_detect` (low risk: read-only)
  - Response enriched mit name/layer aus Neo4j-Fetch (Frontend braucht keine separaten Lookups)
- **+11 Supertests**: happy-path, type-filter, default-data-*, opts-pass-through, sameTypeOnly-toggle, validation-400s, empty-project, service-throws, scanned!=totalElements

### REQ-RED-003 Frontend (Slice 1 Frontend, commit `0eea800`)
- **Hook `useRedundancies`** mit manual fetch (kein auto-poll), full opts-passthrough, state-machine idle/loading/done/error, **nutzt authFetch**
- **Component `RedundancyPanel`** (Modal):
  - Header: "Scanned X of Y elements · Z candidates found"
  - Side-by-side Element-Cards pro Pair
  - Score-Badge mittig: SAME green / SIMILAR yellow + Prozent
  - Type-Filter-Chips dynamisch
  - Refresh-Button
  - Empty-State mit 🎉
  - **Click auf Paar → `fitToScreen([a, b])` + selectElement(a)**
- **Integration**: GitMerge-Icon in Toolbar (neben Heal/X-Ray), conditional auf projectId
- Bug-Fix nebenbei: `authFetch.test.ts` `global` → `globalThis` (tsc-strict)

---

## Toolchain-Pitfall identifiziert + gelöst

Mitten in Slice-1 Test-Setup gestolperter Babel-Parser-Fehler:
```
SyntaxError: Unexpected token ":"
Test Suites: 1 failed, 1 total | Tests: 0 total
```
Auch das gestern-grüne `architecture.routes.similar-api.test.ts` scheiterte plötzlich identisch. War ein **stale Jest-Cache**. Fix: `npx jest --clearCache`. **30 Sekunden** statt der befürchteten 1-2h Toolchain-Debug-Session.

Gehört in den nächsten Onboarding-Doc: bei jedem mysteriösen "TypeScript-stolpert"-Fehler erst Jest-Cache wipen.

---

## End-to-End-Validation auf Production

**BSH-ESG-Compliance-Transformation** geöffnet, GitMerge-Icon in Toolbar geklickt:

| | |
|---|---|
| Scanned | 15 of 61 elements |
| Pairs found | 20 candidates |
| Top SAME | Supplier-Master ↔ Supplier-Contact-Information · **85%** |
| Cluster-Erkenntnis | ESG-Data-Collection-Request taucht 3× in Top-10 → Wildwuchs |
| Audit-Log-Trio | Reporting-Cycle-Audit-Log / Risk-Assessment-Audit-Log / Risk-Score-Report → Konsolidierungs-Kandidat |
| Click-to-Zoom | ✓ funktioniert (PropertyPanel öffnete sich auf ESG-Data-Collection-Request, sichtbar in Sidebar) |

Genau die EAM-Story die wir in Linear/Strategy-Doc als USP versprochen hatten.

---

## Cumulative

```
Tests:      Server 110 + Client 130 = 240 grün
Commits:    686754e · 0aab456 · 0eea800 + 1 Doc-Commit pending
Linear:     THE-245 + THE-247 = Done
            THE-244 (Parent) + THE-246/249/250 = In Progress / Backlog
```

---

## Sprint-3-Status

```
Tag 1 ✓ Token-Bug + UC-RED-001 Slice 1 (Endpoint + UI) live
Tag 2  REQ-RED-002 Cross-Type Pairs (THE-246)
Tag 3  REQ-RED-004 Bulk-Merge-Modal (THE-249) — destruktiv, careful
Tag 4  REQ-RED-005 Audit (THE-250) + ggf. PLATEAU-Polish nebenbei
Tag 5  Spike Cross-Workspace für UC-HARM-001 + ADR
```

---

## Linear-Backlog

- THE-244 Parent UC-RED-001 — In Progress (1 von 5 REQs offen, 2 done, 3 to do)
- THE-246 REQ-RED-002 Cross-Type Pairs — Backlog → Tag 2
- THE-249 REQ-RED-004 Bulk-Merge-Modal — Backlog → Tag 3
- THE-250 REQ-RED-005 Audit + Telemetrie — Backlog → Tag 4

---

## Bekannte offene Beobachtungen aus Production

Aus dem Screenshot fielen ein paar Cluster auf die wir später diskutieren könnten:
- **ESG-Data-Collection-Request** ist 3× in Top-10 → vermutlich überlappt zu viel mit anderen ESG-Reporting-Elements. UC-RED-001 macht das sichtbar. UC-HARM-001 (cross-project) könnte hier auch Patterns erkennen.
- **Reporting-Cycle-Audit-Log / Risk-Assessment-Audit-Log / Risk-Score-Report** Cluster — kandidaten für REQ-RED-002 Cross-Type-Pairs (alle haben unterschiedlichen Type aber semantisch zusammengehörig).
