# UC-ICM-002 D5 — Production Deploy Status (2026-05-22)

**Stand:** ⚠️ teilweise blockiert auf Hostinger-API-Side

## Was erledigt ist

| Aktion | Status |
|---|---|
| `claude/inspiring-carson-42c16f` → `master` fast-forward push | ✅ |
| GitHub Actions Workflow für `Build & Deploy` getriggert | ✅ (manuell, push-trigger funktionierte nicht) |
| GHCR-Build-Bug gefixt (lowercase image name) | ✅ Commit `9de9376` |
| Docker-Image gebaut + zu `ghcr.io/maceeeee/thearchitect:latest` gepusht | ✅ |
| **Deploy-Step zur Hostinger-API** | ❌ `curl -sf` exit code 22 |

## Was blockiert

### Hostinger-Deploy-Step (run 26294269683 job `deploy`)

```
curl -sf -X POST \
  -H "Authorization: ***" \
  -H "Content-Type: application/json" \
  -d "$PAYLOAD" \
  "https://developers.hostinger.com/api/vps/v1/virtual-machines/$HOSTINGER_VM_ID/docker"
##[error]Process completed with exit code 22.
```

`curl -sf` exit 22 = HTTP 4xx/5xx vom Hostinger-API. `-s` blendet
den Response-Body aus → kein konkreter Error sichtbar.

**Mögliche Ursachen:**
1. `HOSTINGER_API_KEY` ist abgelaufen / rotiert
2. `HOSTINGER_VM_ID` falsch
3. API-Endpoint-Schema von Hostinger seit März 2026 geändert
4. Image-Pull-Probleme (private GHCR — VPS-Zugriff?)

### Auth-Gate verhindert HTTP-Smoke-Test der neuen Endpoints

Production läuft `authenticate` Middleware **vor** der Route-Resolution.
Daher gibt `/api/projects/X/compliance/mappings/auto` **401** zurück —
ob die Route existiert oder nicht. Pre-Deploy- und Post-Deploy-Smoke
sehen identisch aus.

Smoke-Probe-Output (Baseline VOR Deploy):
```
✅ HTTP 200  app health
✅ HTTP 401  W1 regulations crawler-health (existed pre-W2)
✅ HTTP 401  W2 compliance/mappings/auto
✅ HTTP 401  W2 compliance/mappings/preview
✅ HTTP 401  W2 compliance/mappings/by-element
✅ HTTP 401  W2 compliance/mappings/by-regulation
✅ HTTP 401  W2 compliance/mappings/confirm
```

⚠️ **Diese 7/7 PASS beweisen NICHTS** — bogus routes geben auch 401 zurück.

## Was als nächstes getan werden muss

### Option 1 — Hostinger-API-Pfad reparieren (User-Action)

1. Prüfen ob `HOSTINGER_API_KEY` noch gültig (GH-Secrets):
   ```bash
   curl -sS -X GET \
     -H "Authorization: Bearer $KEY" \
     "https://developers.hostinger.com/api/vps/v1/virtual-machines"
   ```
2. Falls 401: neuen API-Key in [Hostinger Console](https://hpanel.hostinger.com) generieren
3. GH-Secret aktualisieren:
   ```bash
   gh secret set HOSTINGER_API_KEY -R MaCeeeee/TheArchitect
   ```
4. Workflow neu triggern:
   ```bash
   gh workflow run "Build & Deploy" --ref master
   ```

### Option 2 — Manueller rsync-Deploy (User-Action, aus Memory)

```bash
# Mac:
rsync -avz --delete \
  --exclude 'node_modules' --exclude 'dist' \
  --exclude '*.tsbuildinfo' --exclude '.turbo' \
  --exclude '*.log' --exclude '.env*' --exclude '.git' \
  /Users/mac_macee/javis/packages/ \
  root@76.13.150.49:/docker/thearchitect/packages/

# VPS:
ssh root@76.13.150.49 'cd /docker/thearchitect && \
  docker compose -f docker-compose.prod.yml up -d --build --force-recreate app'

# Smoke nach 30s:
ssh root@76.13.150.49 'sleep 30 && \
  docker compose -f /docker/thearchitect/docker-compose.prod.yml logs --tail=20 app \
  | grep -E "Server running|seed|Failed"'
```

### Option 3 — Authenticated Post-Deploy E2E

Nach erfolgreichem Deploy (egal welcher Pfad):
1. Login auf thearchitect.site mit Production-User
2. JWT aus DevTools kopieren (oder via `/api/auth/login` API-Call)
3. Aufruf:
   ```bash
   curl -s -X POST https://thearchitect.site/api/projects/<bsh-project-id>/compliance/mappings/auto \
     -H "Authorization: Bearer <JWT>" \
     -H "Content-Type: application/json" \
     -d '{}'
   ```
4. Erwarteter Output bei erfolgreichem Deploy:
   ```json
   { "success": true, "data": { "total": 16, "mapped": ~50, "errors": [], "durationMs": ~30000 } }
   ```

## Lokale Vorarbeit als Backup-Plan

Falls Production-Deploy heute nicht klappt, ist `Option D` aus dem
D5-Plan eine valide Alternative: lokale Seed der 16 Regs + BSH-Architecture
und vollständiger Auto-Mapping-Run.

Vorhandene Voraussetzungen:
- ✅ Lokale Neo4j läuft (`thearchitect-neo4j`)
- ✅ Lokale Mongo läuft (`thearchitect-mongodb`, hat 152 users + 130 projects)
- ❌ 16 Regulations sind nur in Production, lokal 0 → bräuchte ein Seed-Script

## Heute geschafft (D5-Teilumfang)

- ✅ Master-Merge + Push (5 + 1 commits)
- ✅ GH-Actions-Workflow gefixt (lowercase image tag)
- ✅ Docker-Image gebaut + verpackt
- ✅ Production-Smoke-Script + ehrliche Diagnose
- ⏳ Hostinger-API-Deploy steht aus (User-Action erforderlich)

## Verbleibende AC-Coverage für THE-280

| AC | Beweis | Status |
|---|---|---|
| AC-1 Auth-protected | Unit-Tests + Production-401 | ✅ |
| AC-2 Performance < 90s | Live-Benchmark (5.23×, 30s @ c=5) | ✅ |
| AC-3 Audit-Entry | Unit-Tests | ✅ |
| AC-4 Rate-Limit Preview | Code-Path verifiziert | ✅ |
| AC-5 5 BSH-Demo-Szenarien | Live Anthropic (5/5 @ 0.95) | ✅ |
| AC-Production-Live | Hostinger-Deploy | ⏳ blocked |
