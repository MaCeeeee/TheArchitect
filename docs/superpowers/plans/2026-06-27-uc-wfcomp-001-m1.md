# UC-WFCOMP-001 — M1 (Mechanismus-Beweis) Implementation Plan

> **For agentic workers:** Use superpowers:subagent-driven-development (subagents available) or superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Deterministischer End-to-End-Pfad „n8n-Workflow rein → rote/grüne Art.-30-Lückenliste raus", evaluiert gegen Golden-Fixtures — ohne LLM, ohne Queue, ohne Politur.

**Architecture:** Additiv auf der ICM/REQGEN-Schiene (`Regulation`+`ComplianceRequirement`). Zwei neue Felder (`criticality`, `traceTarget`) auf `ComplianceRequirement`. Seed der 7 Art.-30-Felder. Sanitize-Grenze vor Ingestion. Lift-Pass (2. Pass über importierten Graphen, Connector untouched) + Trace-Check-Cypher auf dem realen `:ArchitectureElement{type}`-Modell. Eval-Harness als Ziel, das M1 erfüllt.

**Tech Stack:** TypeScript Monorepo (shared/server), Mongoose (MongoDB), Neo4j (Bolt), Jest + ts-jest.

**RVTM:** `docs/superpowers/rvtm/2026-06-27-uc-wfcomp-001-rvtm.md`
**Success-Kriterien:** `docs/superpowers/2026-06-27-uc-wfcomp-001-success-criteria.md`
**Linear:** THE-351 (Parent) · M1-Slices: THE-359 (.7 Eval), THE-352 (.1 Seed), THE-358 (.0 Sanitize), THE-353 (.2 Lift, min.), THE-355 (.4 Trace, min.)

---

## Design-Entscheidung (vor Task 1 bestätigen)

**`criticality` + `traceTarget` leben auf `ComplianceRequirement` (Instanz), nicht in einem neuen Template-Store.**
- Begründung: `Regulation` UND `ComplianceRequirement` sind im bestehenden Schema bereits `projectId`-scoped (UC-ICM crawlt pro Projekt). Der Art.-30-Satz wird also **pro Projekt geseedet** — konsistent mit dem vorhandenen Muster, niedrige Change-Amplification.
- **YAGNI:** Eine Template-Ebene (kanonischer Satz, projektunabhängig, für DORA/NIS2-Skalierung) wird **bewusst aufgeschoben**. Erst extrahieren, wenn die 2. Regulierung real ansteht — sonst vorzeitige Abstraktion (Ousterhout: Komplexität durch Abhängigkeiten).
- **Komplexitäts-Watch-Point:** `traceTarget` als Mini-DSL = die eine neue kognitive Last. Mitigation: die 3 strukturierten Beispiele aus THE-352 als Doc-Kommentar am Typ.

---

## Chunk 1: Foundation (Schema + Seed + Harness-Gerüst)

### Task 1: Neue Felder `criticality` + `traceTarget`

**Files:**
- Modify: `packages/shared/src/types/compliance.types.ts` (ComplianceRequirementDTO + neue Typen)
- Modify: `packages/server/src/models/ComplianceRequirement.ts` (Schema)
- Test: `packages/server/src/__tests__/ComplianceRequirement.model.test.ts`

- [ ] **Step 1: Failing test — Modell akzeptiert criticality + traceTarget**

```ts
// in ComplianceRequirement.model.test.ts
it('persists criticality and traceTarget round-trip', async () => {
  const doc = await ComplianceRequirement.create({
    projectId: new mongoose.Types.ObjectId(),
    regulationId: new mongoose.Types.ObjectId(),
    title: 'Zweck(e) der Verarbeitung angeben',
    description: 'Der Zweck jeder Verarbeitungstätigkeit muss benannt sein.',
    priority: 'must',
    createdBy: 'human',
    criticality: 'HART',
    traceTarget: { from: 'process', steps: [{ rel: 'realization', to: 'goal', where: { kind: 'Purpose' } }] },
  });
  const reloaded = await ComplianceRequirement.findById(doc._id).lean();
  expect(reloaded?.criticality).toBe('HART');
  expect(reloaded?.traceTarget?.steps[0].rel).toBe('realization');
});
```

- [ ] **Step 2: Run — verify FAIL**

Run: `cd packages/server && npx jest ComplianceRequirement.model -t "criticality and traceTarget"`
Expected: FAIL (Felder werden von Mongoose gestript / Typ kompiliert nicht).

- [ ] **Step 3: Add shared types**

```ts
// compliance.types.ts — neben ComplianceRequirementDTO
export type Art30Criticality = 'HART' | 'BEDINGT' | 'WEICH';

export interface TraceStep {
  rel: string;                       // ConnectionType, z.B. 'assignment' | 'realization' | 'access'
  to: string;                        // ElementType oder '*'
  where?: Record<string, unknown>;   // Knoten-Constraints, z.B. { kind: 'Purpose' }
}

/**
 * Maschinenlesbarer Trace-Pfad für REQ-WFCOMP-001.4 (THE-355).
 * Beispiele (siehe THE-352):
 *   a: { from:'process', steps:[{rel:'assignment', to:'business_role', where:{role:'Controller'}}] }
 *   e (BEDINGT): { from:'process', guard:{flag:'thirdCountry',equals:true},
 *        steps:[{rel:'flow',to:'business_role',where:{role:'Recipient'}},{rel:'association',to:'*',where:{kind:'Safeguard'}}] }
 *   g (cross-layer): { from:'data_object', where:{personal:true},
 *        steps:[{rel:'access',to:'application_component'},{rel:'serving',to:'node'},{rel:'association',to:'requirement',where:{kind:'TOM',art32:true}}] }
 */
export interface TraceTarget {
  from: string;
  where?: Record<string, unknown>;
  guard?: { flag: string; equals: unknown };  // bedingt (lit. e): greift nur wenn flag === equals
  steps: TraceStep[];
}
```
Und in `ComplianceRequirementDTO` ergänzen:
```ts
  criticality?: Art30Criticality;   // WFCOMP: Art.-30-Klasse (HART/BEDINGT/WEICH)
  traceTarget?: TraceTarget;        // WFCOMP: erwarteter Graph-Pfad
```

- [ ] **Step 4: Add Mongoose schema fields**

```ts
// ComplianceRequirement.ts — im Schema-Block, nach mappingRationale
criticality: { type: String, enum: ['HART', 'BEDINGT', 'WEICH'], required: false },
traceTarget: { type: Schema.Types.Mixed, required: false },
```
Und im `IComplianceRequirement`-Interface die zwei optionalen Felder ergänzen (Typen aus shared importieren).

- [ ] **Step 5: Run — verify PASS**

Run: `cd packages/server && npx jest ComplianceRequirement.model`
Expected: PASS (alle, inkl. bestehende).

- [ ] **Step 6: Build shared + commit**

```bash
npm run build -w @thearchitect/shared
git add packages/shared/src/types/compliance.types.ts packages/server/src/models/ComplianceRequirement.ts packages/server/src/__tests__/ComplianceRequirement.model.test.ts
git commit -m "feat(wfcomp): criticality + traceTarget on ComplianceRequirement (REQ-WFCOMP-001.1)"
```

---

### Task 2: Art.-30-Seed (1 Regulation + 7 ComplianceRequirements)

**Files:**
- Create: `packages/server/src/data/art30.seed-data.ts` (Konstanten: Verbatim + 7 Specs)
- Create: `packages/server/src/scripts/seed-art30.ts` (idempotenter Upsert pro projectId)
- Test: `packages/server/src/__tests__/seed-art30.test.ts`

**Datenquelle:** Verbatim-Wortlaut amtlich verifiziert (CELEX 32016R0679, OJ L 119/50–51) — exakt aus THE-352 übernehmen. `createdBy:'human'`, `source:'dsgvo'`, `paragraphNumber:'Art. 30 Abs. 1'`, `effectiveFrom:'2018-05-25'`, `version:1`.

- [ ] **Step 1: Failing test**

```ts
it('seeds 1 Regulation + 7 ComplianceRequirements with correct criticality split', async () => {
  const projectId = new mongoose.Types.ObjectId().toString();
  await seedArt30(projectId);
  const regs = await Regulation.find({ projectId, source: 'dsgvo' });
  const reqs = await ComplianceRequirement.find({ projectId });
  expect(regs).toHaveLength(1);
  expect(reqs).toHaveLength(7);
  expect(reqs.filter(r => r.criticality === 'HART')).toHaveLength(4);   // a–d
  expect(reqs.filter(r => r.criticality === 'BEDINGT')).toHaveLength(1); // e
  expect(reqs.filter(r => r.criticality === 'WEICH')).toHaveLength(2);   // f,g
  expect(reqs.every(r => r.traceTarget?.from)).toBe(true);
  expect(reqs.every(r => r.createdBy === 'human')).toBe(true);
});

it('is idempotent (re-run yields no duplicates)', async () => {
  const projectId = new mongoose.Types.ObjectId().toString();
  await seedArt30(projectId);
  await seedArt30(projectId);
  expect(await ComplianceRequirement.countDocuments({ projectId })).toBe(7);
});
```

- [ ] **Step 2: Run — verify FAIL** (`seedArt30` not defined).

- [ ] **Step 3: Seed-Daten + Funktion**
  - `art30.seed-data.ts`: `ART30_FULLTEXT` (Verbatim a–g), `ART30_FIELDS` = Array von 7 `{ litera, title, sourceParagraph, criticality, priority, traceTarget }` exakt aus THE-352-Tabelle.
  - `seed-art30.ts`: `export async function seedArt30(projectId: string)` — `Regulation.findOneAndUpdate({projectId,source:'dsgvo',paragraphNumber:'Art. 30 Abs. 1'}, {...}, {upsert:true,new:true})`, dann je Feld `ComplianceRequirement.findOneAndUpdate({projectId, regulationId, title}, {...}, {upsert:true})`. CLI-Runner am Dateiende (wie `backfill-provenance.ts`).

- [ ] **Step 4: Run — verify PASS.**

- [ ] **Step 5: Commit**
```bash
git commit -am "feat(wfcomp): Art.30 seed — 1 Regulation + 7 ComplianceRequirements (REQ-WFCOMP-001.1)"
```

---

### Task 3: Eval-Harness-Gerüst + erste 3 Fixtures (THE-359 Foundation)

**Files:**
- Create: `packages/server/src/__tests__/fixtures/wfcomp/clean-compliant.json` (n8n-Workflow-Definition)
- Create: `packages/server/src/__tests__/fixtures/wfcomp/missing-purpose.json`
- Create: `packages/server/src/__tests__/fixtures/wfcomp/pindata-leak.json` (mit `pinData` + Klarnamen)
- Create: `packages/server/src/__tests__/fixtures/wfcomp/ground-truth.ts` (erwartete Lücken pro Fixture, pro lit.)
- Create: `packages/server/src/__tests__/wfcomp-eval.test.ts` (Harness-Gerüst + Gate-Report-Struktur)

- [ ] **Step 1: Fixtures + Ground-Truth anlegen.** Minimal valide n8n-JSON (`nodes[]`+`connections{}`). `ground-truth.ts` exportiert pro Fixture `{ gdprScope: boolean, expectedGaps: {litera, criticality}[], expectsNoPiiAtRest?: boolean }`.

- [ ] **Step 2: Harness-Gerüst — Gate-Report-Struktur, Gates G1–G5 als `it.todo`/pending.** Zunächst nur Assertion „alle Fixtures parsen als valide n8n-JSON" grün; G1–G5 als `todo` markiert (werden in Task 4–6 scharf geschaltet). Das etabliert das Ziel sichtbar.

- [ ] **Step 3: Run** `npx jest wfcomp-eval` → grün (Parse) + Pending-Liste der Gates.

- [ ] **Step 4: Commit**
```bash
git commit -am "test(wfcomp): eval-harness scaffold + 3 fixtures + ground-truth (REQ-WFCOMP-001.7)"
```

---

## Chunk 2: Deterministischer Pfad (.0 → .2 → .4) — Umriss, scharf nach Foundation

> Diese drei Tasks bekommen ihren bite-sized-TDD-Detailplan **nach** Chunk 1 — erst dann lese ich gezielt die Parse-Oberfläche (`upload.service.ts` / `n8n.connector.ts`) und die Neo4j-Query-Helfer, um ehrlichen statt erfundenen Code zu schreiben. Hier die fixen Design-Entscheidungen, damit Chunk 1 dagegen baut:

### Task 4 (.0 Sanitize, THE-358) — blockt 5/6
- Neuer `sanitizeN8nDefinition(raw)` **vor** jeder Persistenz: behält Node-Typen/Topologie/Domains/Feld-**Namen**; verwirft Werte, `pinData`, Credentials, Freitext. **Test zuerst** gegen `pindata-leak.json`: nach Sanitize 0 Klarnamen/PII im Output (Gate G1). Kein Roh-JSON at-rest.

### Task 5 (.2 Lift-min, THE-353)
- `liftComplianceSemantics(projectId)` als **2. Pass** über den importierten Graphen. M1-Minimal: nur **lit. d (Empfänger via Ziel-Domain)** + **lit. g (Storage-Existenz via DB-Node)** + `gdprScope`-Flagging. `provenance:'import'`. Idempotent (MERGE). Connector untouched.

### Task 6 (.4 Trace-min, THE-355)
- `runArt30TraceCheck(projectId)` traversiert die `traceTarget`-Specs auf `:ArchitectureElement{type}`/`:CONNECTS_TO{type}`. M1: HART-Felder + BEDINGT-Guard + `collect()`/`all()` gegen Multi-DataObject-False-OK (Gate G4/G5). Gruppierung HART/BEDINGT/WEICH. **Schaltet G2–G5 scharf.**

**M1 fertig ⟺ Eval-Harness Gates G1–G5 grün** (Success-Doc).

---

## Offene Bestätigungen vor Code
1. **Design-Entscheidung oben** (criticality/traceTarget auf Instanz, Template-Ebene deferred) — ok?
2. **Worktree:** M1 in dediziertem Worktree (`using-git-worktrees`) oder Feature-Branch im Haupt-Repo? (Dev-Server-Port-Falle aus früheren Sessions beachten.)
3. Chunk-2-Detailplan **nach** Chunk-1-Landing, mit gezieltem Read der Lift/Trace-Oberfläche.
