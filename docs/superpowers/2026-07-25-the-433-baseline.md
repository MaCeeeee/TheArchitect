# Nachweis — THE-433 Relations-Baseline (dokumentierte Grenze)

**Stand:** 2026-07-25 · **Golden:** `relations.v4.json` (175 Fälle, frozen) · **Prompt:** rp-2
**Klassifikator:** claude-haiku-4-5 (= `RELATIONS_BATCH_MODEL`, der Prod-Batch)

> **Verdikt: ABBRUCH nach vorab fixierter Regel — es werden KEINE Vorschläge in den Korpus geschrieben.**
> Das Tor hat funktioniert: Der Klassifikator ist stark beim „gibt es eine Kante?" (none-Precision 96,2 %),
> aber unter der Schwelle bei der feinen Typisierung (INTERPRETS-F1 0,476 < 0,70). Per Paper §5 ein
> vollwertiges, dokumentiertes Ergebnis.

## Die Zahlen

| Metrik | Wert | Gate |
|---|---|---|
| Gesamt-Übereinstimmung (Typ UND Richtung) | 161/175 = **92,0 %** | ≥ 0,85 ✅ |
| `none`-Precision (Negativ-Klasse, n=155) | **96,2 %** (R 98,1 %) | ≥ 0,90 ✅ |
| **INTERPRETS-F1** (n=10, einzige gegatete Positiv-Klasse) | **0,476** (P 45,5 % · R 50,0 %) | ≥ 0,70 ❌ |
| Metadaten-Typ-Vorschläge | **0** | = 0 ✅ |
| Messausfälle | 0 | — |

Fehler-Aufschlüsselung der 14 Gesamt-Fehler: 12 Typ-Fehler + 2 Richtungs-Fehler
(`cra-en-art-3__nis2-art-6`, `dsgvo-art-4__nis2-de-art-35` — beide geliehene Definitionen, wo die
„Richtung ab definierender Norm"-Regel subtil ist).

Dünne Positiv-Klassen (ausgewiesen, NICHT gegated, n<10): CONCRETIZES F1 0,444 (n=5) ·
RECOGNIZES_EQUIVALENCE F1 0,500 (n=3) · PREVAILS_OVER F1 1,000 (n=1) · SETS_PARAMETER F1 0,000 (n=1).

## Diagnose

1. **Haiku trennt „Beziehung / keine Beziehung" zuverlässig** (none P 96,2 %) — das ist der dominante
   Teil (155/175) und für einen Vorschlags-Filter das Wichtigste.
2. **Die feine Beziehungs-Typisierung ist zu schwach.** Bei INTERPRETS über- UND unter-typisiert das
   Modell zugleich (11 Vorschläge, davon 5 richtig; 5 echte verpasst). Das ist die Paper-Warnung
   (non-taxonomic RE domänen-variabel) empirisch belegt.
3. **Die gegatete Klasse steht bei n=10** — direkt an der Schwelle. Bei so wenigen Fällen ist die
   F1-Zahl volatil; ein belastbareres INTERPRETS-Urteil braucht mehr Fälle (zweiter Mining-Lauf,
   THE-517-Folge).

## Konsequenz

- **Kein Korpus-Write.** Die Pipeline steht (Slice 1 gebaut, getestet, deployt), aber der Prod-Batch
  bleibt aus, bis das Tor fällt. Suggest-only wäre erst nach bestandenem Gate `dark` erlaubt.
- **Kein Prompt-Tuning nach der Messung** (Freeze-Ratsche). Die Zahl steht.

## Optionen (Architekten-Entscheid, nicht in diesem Dokument gefällt)

1. **Als dokumentierte Grenze akzeptieren** — Slice 1 = „Pipeline steht, Haiku reicht für die Kanten-
   Typisierung noch nicht, Tor schützt". Ehrlichstes Ergebnis, sauberste Story.
2. **Stärkeres Batch-Modell** (z. B. Sonnet) — `RELATIONS_BATCH_MODEL` ändern, **neu** baseline-messen
   (kein Tuning: Golden + Prompt unverändert, nur das Modell). Empirische Frage, ob ein größeres Modell
   die feine Typisierung hebt.
3. **INTERPRETS verdicken** (2. Mining-Lauf CRA↔KI-VO/MDR) → n≥10 belastbar → neu messen. Hebt Haikus
   Fähigkeit nicht, gibt aber ein verlässlicheres Urteil.

Tokens: 501.786 in / 32.877 out.
