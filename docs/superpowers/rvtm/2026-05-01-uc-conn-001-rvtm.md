# RVTM — UC-CONN-001 Auto-Heal Isolated ArchiMate Connections

**Spec:** `docs/superpowers/plans/2026-05-01-uc-conn-001-auto-heal-connections.md`
**Linear UC:** [THE-211](https://linear.app/thearchitect/issue/THE-211)
**Created:** 2026-05-01
**Status:** Plan-stage; implementation not started

## Traceability Matrix

| ID | Requirement (source) | Plan Task(s) | File(s) | Verification | Status |
|---|---|---|---|---|---|
| **REQ-01-AC1** | New file `connectionSuggestion.service.ts` with `suggestConnectionsForIsolatedElements` export ([THE-212](https://linear.app/thearchitect/issue/THE-212)) | Task 3 | `packages/server/src/services/connectionSuggestion.service.ts` | Unit test (Task 2 — module loads) | Pending |
| **REQ-01-AC2** | Reuses `getValidRelationships()` + `getDefaultRelationship()` from `archimate-rules.ts` | Task 3 (Step 3.1, imports + `scorePair`) | same | Code review + unit test on relationship-type output | Pending |
| **REQ-01-AC3** | Confidence-Score: `0.5 * layerCompat + 0.5 * aspectMatch` (embedding deferred) | Task 3 (Step 3.1, `scorePair` function) | same | Unit test asserts confidence ∈ (0,1] | Pending |
| **REQ-01-AC4** | Pre-filter via Layer-Compatibility avoids O(n²) blowup | Task 3 (per-element loop with early-skip on connected pairs) | same | Manual benchmark on BSH workspace (Task 10.3) | Pending |
| **REQ-01-AC5** | Reads existing connections, only suggests missing pairs | Task 3 (`connectedPairs` Set + `pairKey`) | same | Unit test: "skips already-connected elements" (Task 2) | Pending |
| **REQ-01-AC6** | Return type `HealReport { elementsAnalyzed, suggestionsTotal, perElement: Map }` | Task 3 (interface declarations) | same | TypeScript compile + unit test asserts shape | Pending |
| **REQ-01-AC7** | Unit tests for confidence + edge cases | Task 2 (now 7 test cases — added empty-workspace, unknown-type, includeWeak per review) | `__tests__/connectionSuggestion.service.test.ts` | `npx jest` green | Pending |
| **REQ-02-AC1** | New file `architecture.routes.ts` extension (note: NOT a separate file — extending the existing route file is more idiomatic) | Task 4 | `packages/server/src/routes/architecture.routes.ts` | Code review + manual smoke test | Pending |
| **REQ-02-AC2** | Route `POST /api/architecture/:projectId/heal-connections` (NB: project-scoped — corrected from Linear wording, no workspaceId) | Task 4 (Step 4.2) | same | curl smoke test (Step 4.3) | Pending |
| **REQ-02-AC3** | `mode=dryRun` returns report without persistence | Task 4 (Step 4.2, `if body.mode === 'dryRun'` branch) | same | Manual smoke + Cypher count unchanged | Pending |
| **REQ-02-AC4** | `mode=apply` persists in **Neo4j only** (correction — connections are not in MongoDB) | Task 4 (Step 4.2, `runCypher CREATE`) | same | Cypher post-check shows new edges with `source: 'ai-heal'` | Pending |
| **REQ-02-AC5** | `?minConfidence=0.7` query param (in body for this impl — corrected) | Task 4 (Step 4.2, Zod schema) | same | curl with `{minConfidence:0.5}` and `{minConfidence:0.95}` differ | Pending |
| **REQ-02-AC6** | Auth + role-check via `requirePermission(PERMISSIONS.CONNECTION_CREATE)` | Task 4 (Step 4.2, middleware chain) | same | Token without permission → 403 | Pending |
| **REQ-02-AC7** | Audit-log per apply via existing `audit({ action: 'heal_connections' })` | Task 4 (Step 4.2, middleware chain) | same | Mongo `auditlogs` query (Task 10.3) | Pending |
| **REQ-02-AC8** | Rate-limit max 1 apply per workspace per 5 min | Task 4 (Step 4.2 note) | same — *deferred if middleware does not support per-route key* | Two consecutive applies; second 429 — only verify if shipped | **Conditional** |
| **REQ-02-AC9** | Error handling: 404 / 403 / 429 | Task 4 (Step 4.2, `try/catch` + Zod) | same | curl with bad payload / wrong role | Pending |
| **REQ-02-AC10** | Route registered in `index.ts` under `/api` | Task 4 — automatic via `architecture.routes.ts` already mounted at `/api/architecture` | `packages/server/src/index.ts` | Reading the existing line confirms | **Already-done (existing mount)** |
| **NEW REQ-02-AC11** *(added per review)* | Apply-mode is idempotent — double-clicks / re-runs do not create duplicate edges | Task 4 (Step 4.2: two-step exists-then-MERGE on `(sourceId, targetId, type)`) | same | Supertest in Task 4b + manual: run apply twice, check `appliedCount` is 0 on second call | Pending |
| **NEW REQ-02-AC12** *(added per review)* | Apply-mode runs all writes in a single Cypher round-trip via UNWIND | Task 4 (Step 4.2 batch) | same | Manual benchmark: 50×4 BSH suggestions complete in <2s | Pending |
| **NEW REQ-02-AC13** *(added per review)* | Supertest integration tests: dryRun, empty-whitelist apply, validation 400 | Task 4b (3 cases) | `packages/server/src/routes/__tests__/healConnections.routes.test.ts` | `npx jest healConnections` green | Pending |
| **REQ-03-AC1** | `PropertyPanel.tsx:704-753 handleConnect` calls `architectureAPI.createConnection` | Task 7 (Step 7.1) | `packages/client/src/components/ui/PropertyPanel.tsx` | Manual click + reload (Step 7.3) | Pending |
| **REQ-03-AC2** | Local store update on success | Task 7 (Step 7.1) | same | Manual: connection appears in 3D after click | Pending |
| **REQ-03-AC3** | Toast on error, no state update | Task 7 (Step 7.1, `catch` branch) | same | Force a 500 (e.g. malformed source) → toast appears, store unchanged | Pending |
| **REQ-03-AC4** | Loading state / disabled button during call | Task 7 (Step 7.1 — *current implementation uses optimistic update; loading state is implicit via temp-id*) | same | Manual | Pending |
| **REQ-03-AC5** | Optimistic update with rollback | Task 7 (Step 7.1, temp-id pattern) | same | Force a 500 → temp connection disappears | Pending |
| **REQ-03-AC6** | Workspace-reload preserves connection | Task 7 (Step 7.3) | same | Manual reload | Pending |
| **REQ-04-AC1** | `blueprint.service.ts` Default `applyConnections: true` | Task 5 (Step 5.2) | `packages/server/src/services/blueprint.service.ts` | Read default after change; test by generating new blueprint | Pending |
| **REQ-04-AC2** | Bulk-Insert in Neo4j after `connections_ready` (now: single UNWIND-MERGE roundtrip per review) | Task 5 (Step 5.2) | same | Generated blueprint → Cypher count > 0; one Neo4j call regardless of N | Pending |
| **REQ-04-AC3** | New stream event `connections_persisted` | Task 5 (Step 5.2) | same + `BlueprintStreamEvent` type | Client-side console / DevTools | Pending |
| **REQ-04-AC4** | Opt-out via `applyConnections: false` | Task 5 (Step 5.2) | same | Pass `applyConnections: false` in test request | Pending |
| **REQ-04-AC5** | Validation drops invalid connections via `isValidRelationship` | Task 5 — *uses existing `validateConnections` which already does this* | same | Existing validator behavior | **Already-done** |
| **REQ-04-AC6** | Audit-log per bulk apply (`source: blueprint-auto`) | Task 5 (Step 5.2 — `source` baked into the Cypher property; full audit-log entry deferred — the property serves the same purpose for now) | same | Cypher: `MATCH ()-[r:CONNECTS_TO]->() WHERE r.source = 'blueprint-auto' RETURN count(r)` | **Partial** (audit via property, not AuditLog model) |
| **REQ-04-AC7** | Backward-compat dokumentiert | Task 5 (Step 5.3) — explicit pass `applyConnections: false` for callers without `projectId` | same callers | Code review | Pending |
| **REQ-05-AC1** | "Heal Workspace" button in 3D-Toolbar with Sparkles icon | Task 9 (Step 9.1) | `packages/client/src/components/3d/Toolbar.tsx` *(verify in Task 1.1)* | Manual UI inspection | Pending |
| **REQ-05-AC2** | Button-Click → `dryRun` then opens `HealWorkspaceModal` | Task 9 (Step 9.1, `setHealOpen(true)`) | same | Click button → modal opens, network tab shows POST | Pending |
| **REQ-05-AC3** | New file `HealWorkspaceModal.tsx` | Tasks 8a/8b/8c (split per review) | `packages/client/src/components/copilot/HealWorkspaceModal.tsx` | File exists, type-checks | Pending |
| **REQ-05-AC4** | Modal lists: Element-Name, Suggested-Target, Relationship, Confidence-Bar | Task 8 (Step 8.1, list rendering) | same | Visual inspection in BSH | Pending |
| **REQ-05-AC5** | Per-Suggestion Toggle + Bulk-Accept | Task 8 (Step 8.1, `toggle` + default-accepted state) | same | Click toggle, observe class change | Pending |
| **REQ-05-AC6** | Confidence-Slider 0.5–1.0, Default 0.7 | Task 8 (Step 8.1, `<input type="range">`) | same | Drag slider, list filters | Pending |
| **REQ-05-AC7** | Apply uses whitelist of accepted | Task 8 (Step 8.1, `apply` builds `whitelist` from non-rejected) | same + Task 4 endpoint | curl trace shows whitelist | Pending |
| **REQ-05-AC8** | Loading bar + success toast | Task 8 (Step 8.1, `applying` state + `toast.success`) | same | Apply, observe toast | Pending |
| **REQ-05-AC9** | 3D-Scene refreshed after apply, Health green | Task 9 (Step 9.1, `onApplied` triggers store refresh) | `Toolbar.tsx` + store | Visual: connections appear, click stakeholder → Health 100% | Pending |
| **REQ-05-AC10** | Modal disabled when no isolated elements | Task 8 (Step 8.1 — empty-list message; button-state polish in Task 9 follow-up) | same | Run on a fully-connected project | Pending |

## Out-of-Scope (tracked, not in this plan)

| Item | Reason |
|---|---|
| Embedding-similarity in confidence formula | REQ-01-AC3 marks it as "(optional)"; defer until empirical data shows the layer+aspect score is insufficient |
| Cross-workspace heal | Out-of-Scope of UC-CONN-001 |
| AI-Refine connection types (e.g. upgrade `association` → `serving`) | Out-of-Scope of UC-CONN-001 |
| Auto-delete misplaced connections | Out-of-Scope (only Add, never Remove — protects user edits) |

## Demo-Day Fallback Path

If the heal endpoint is not stable by 2026-05-04 EOD, the plan provides a Cypher seed script (Plan §"Demo-Day Fallback") that pre-seeds reasonable connections directly. In that case, only REQ-03 (PropertyPanel bugfix) ships for the demo, and REQ-01/02/04/05 move to post-demo.

## Linear Linkage

| RVTM Block | Linear Issue |
|---|---|
| REQ-01 | [THE-212](https://linear.app/thearchitect/issue/THE-212) |
| REQ-02 | [THE-213](https://linear.app/thearchitect/issue/THE-213) — *requires correction: AC-4 mentions MongoDB; should be Neo4j only* |
| REQ-03 | [THE-214](https://linear.app/thearchitect/issue/THE-214) (Urgent) |
| REQ-04 | [THE-215](https://linear.app/thearchitect/issue/THE-215) |
| REQ-05 | [THE-216](https://linear.app/thearchitect/issue/THE-216) |
| UC | [THE-211](https://linear.app/thearchitect/issue/THE-211) — *requires correction: description mentions MongoDB; should be Neo4j only* |
