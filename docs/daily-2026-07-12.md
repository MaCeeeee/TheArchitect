---
date: 2026-07-12
tags: [daily, the-440, dora-live, the-446, the-447, the-448, operational-governance-engine, the-422, uc-ctxgov-001, governed-retrieval, version-pin, the-430, the-431, uc-onto-001, typing-eval, c-score, the-468, the-470, corpus-observability, uc-law-002]
status: closed
---

# 2026-07-12 — Der Rekordtag: DORA live in Prod, Governance-Engine bis Slice 4, governed retrieval + ONTO-Typing-Eval

## TL;DR

Der bislang größte Tag — **5 gemergte PRs (#52–#56) über drei Initiativen**, dazu der große Korpus-Payoff und Observability-Härtung:

1. **Korpus-Arc geschlossen (THE-440, PR #51):** Der Cutover ist **live auf Prod** — die App liest **347 Korpus-Keys** über einen Read-only-User, Fallback bleibt an. **DORA erstmals live in Prod aus dem Korpus verifiziert** (Embed-Gap THE-466/467 gefixt, das 07-11-Daily entsprechend korrigiert). Damit ist die mehrtägige Kette *Kanonisierung → DORA null Code → Prod* real geschlossen.
2. **Operational-Governance-Engine: Slices 2→4 an EINEM Tag (THE-446/447/448, PRs #52+#56):** Sentry-Dedup mit stabilem Fingerprint (#52), dann Closed Loop + LLM-Anreicherung + Ops-Notify (#56). Die gestern geborene Engine ist damit **feature-vollständig über alle 4 Slices** — deterministisch, human-gated (Asilomar #16), LLM strikt nur Vorschlag.
3. **Governed Retrieval (THE-422, PR #54 — die aktuelle Branch):** Deep Module `governedRetrieval`, durch das **alle 6 AI-Konsumenten** Korpus-Kontext beziehen — Read-Side-Eligibility + Version-Pin-Gate. Das Read-Side-Gegenstück zu VERLOCK; beantwortet „war die Wissensbasis aktuell/reproduzierbar?".
4. **UC-ONTO-001 Slice 1 (THE-430/431, PR #55):** Norm-Complexity-Score (C_score) + komplette Term-Typing-Eval-Pipeline; Ontologie **1.2.0 → 1.3.0** (obligationKinds = deontisches Tripel).
5. **Korpus-Observability:** `GET /corpus/status` Mongo↔Qdrant-Drift (THE-468, #53) + corpusHealth-Fehlalarm-Fix nach Recreate (THE-470). Plus **UC-LAW-002-Spec** (korpus-weite Discovery, hybrid) geschrieben.

---

## Teil 1 — THE-440: Korpus-Cutover live + DORA in Prod (PR #51)

Der Abschluss des mehrtägigen Korpus-Arcs (Bridge THE-441 → Cutover THE-440):

- **Repo-Seite (PR #51):** `docker-compose.prod.yml` deklariert jetzt `CORPUS_MONGODB_URI` + `CORPUS_STRICT_READS` im `app`-Service — vorher waren die `.env`-Werte reine Substitution und erreichten den Container nie (genau der `corpusUnconfigured`-Fallback beim ersten Recreate).
- **Cutover-Evidenz (Prod):** Vollständigkeits-Check **16/17** referenzierte `regulationKey`s im Korpus (einzige Lücke = Demo-Artefakt `lksg:live-paste`, per App-DB-Fallback gedeckt). Read-only-User `thearchitect_ro` (Rolle `read`) — die App hält **nie** Korpus-Write-Credentials. `APP_RUNTIME_CORPUS_COUNT=347` aus dem recreateten Container. Fallback bleibt AN, `CORPUS_STRICT_READS` = bewusster Folgeschritt.
- **DORA live verifiziert (`4950831`):** DORA (gestern als reine Datenzeile onboarded) ist jetzt in Prod aus dem Korpus sichtbar — **THE-418 AC-2 real demonstriert**. Dabei ein **Embed-Gap gefunden + gefixt (THE-466/467)**; das 07-11-Daily wurde entsprechend korrigiert (nicht geschönt).

## Teil 2 — Operational-Governance-Engine: Slices 2–4 (PRs #52, #56)

Die gestern geborene „eine Engine, zwei Linsen" wuchs heute von Slice 1 auf **feature-vollständig**:

**Slice 2 — Sentry-Dedup (THE-446, PR #52):**
- **Stabiler Fingerprint (AC-2):** `hash(component + errorType + normalisierter Top-Stack-Frame)`, Zeilen/Spalten gestrippt → überlebt Umformulierung **und** Zeilen-Shifts über Releases. Titel nur Fallback.
- **Dedup + occurrence_counter (AC-3):** offene Chain gleichen Fingerprints → kein neuer Defect, `recordOccurrence` schreibt WORM-Row (counter+1), verlinkt Report als `evidence.occurrences[]`. Terminal-Chains (noise/resolved) starten frisch.
- **Urgency-Eskalation (AC-4):** `urgencyFromOccurrences` (log2), Score deterministisch neu berechnet.
- **n8n:** Workflow `Sentry → Register Ingest` (id `3QMcMgiKMh7WsFei`) validiert, **inaktiv** (API-Key als n8n-Credential, nichts im Repo).
- **Bonus:** Auth-Shadowing-Fix (THE-440-Follow-up) — pfadloses `router.use(authenticate)` in `rag.routes.ts` schattete die Public-Health-Endpoints → mit eigenem Test.

**Slice 3 — Closed Loop (THE-447, PR #56):** stabile `chainId` + `firstSeenAt` + `slaDeadline`; **Verify-Closure** (`resolved` nur bei Tests grün UND keiner Recurrence, sonst `reopen`); **Cascade-Close** (Child-Incidents + Parent-Problem); **SLA-Breach** `sweepSla` schlägt human-gated `escalate` vor (idempotent). Routes `POST /:chainId/close`, `/register/sla-sweep`.

**Slice 4 — LLM-Anreicherung + Notify (THE-448, PR #56):** `suggestDuplicates` + `suggestProblemClusters` (nur Kandidaten-Pool, nie Auto-Merge), injizierbarer `LlmClient` (kein Key → degraded, Engine unberührt), Output geloggt mit model + promptHash. `createProblem` (Human-Confirm), `opsNotify` (Slack Block-Kit, wirft nie). **40/40 Register-Tests grün.** *(Isoliert via git-worktree auf origin/master committet — nur die 10 Register-Dateien.)*

## Teil 3 — THE-422: Governed Retrieval (UC-CTXGOV-001.1, PR #54)

Read-Side-Gegenstück zu VERLOCK (THE-306): ein **Deep Module `governedRetrieval.service.ts`**, durch das **jeder** AI-Konsument Korpus-Kontext bezieht. Erzwingt Eligibility (non-stale = current version) + optionalen Version-Pin (aus Mongo, **nie** Qdrant) + Stale-Drop mit Telemetrie.

- **Zwei Enforcement-Pfade:** `governedQuery` (Qdrant-Vektor: droppt Chunks mit `versionHash != current` → `staleDropped`, behält hashlose Legacy-Punkte als `unverifiable` → kein stiller Kontextverlust, Pin überschreibt Text aus der Mongo-Version) + `resolveGovernedRegulations` (strukturierte Mongo-Lesung, Pin via `getRegulationByKeyAndHash`).
- **Alle 6 Konsumenten gated** (AC-4, grep-verifiziert `queryDocuments` nur noch im Modul): 4 RAG-Generatoren + `/rag/query` + AI-Match + requirement-generation.
- **Security-/Korrektheits-Funde unterwegs:** Alt-Bug `getCurrentVersionHashes` nichtdeterministisches last-wins → **max-version-wins** (hätte AC-2 invertiert); **NoSQL-Operator-Injection** über `/rag/query`-Pin → `sanitizePin` + `z.record(z.string())`; AI-Match `_id`-Threading gefixt (sonst leere `ComplianceMapping.regulationId`).
- **107/107 Surface-Tests grün.** Blockt THE-461 (UC-LAW-002 Discovery).
- **Bewusst nicht drin (Handoff-Seams):** der **Chunk-0-Spike** (operative Verifikation: gibt der Data-Server `versionHash` im `chunk.metadata` zurück?) — Code ist dank keep-on-missing-hash-Policy unabhängig sicher; draft/published-Lifecycle (THE-426), Hash-Chain/Checkpoint (THE-424/425), ContextTrace (THE-423).

## Teil 4 — UC-ONTO-001 Slice 1: Typing-Eval + C_score (PR #55)

Die Read-Side-Eval-Fundierung für die ONTO-Suggest-Familie — additiv, keine Signatur geändert.

- **THE-431 Norm-Complexity-Score:** reine deterministische Metrik (kein LLM/IO) über den @eId-Norm-Baum: 5 Metrik-Familien → `log(1+x)` → gewichtet → Sigmoid → [0,1] mit 5 Bändern. *(Tiefen-Achse Gewicht 0.10 bis THE-415 degeneriert — Formel korrekt, aktiviert sich ohne Code-Änderung mit echtem @eId-Baum.)*
- **THE-430 Typing-Eval:** generische Metriken `expectedCalibrationError` (ECE), `fpFnBias`, `leakageAwareSplit`; **E6 `obligationKinds`** (obligation/prohibition/permission), Ontologie **1.2.0 → 1.3.0** (deontisches Tripel als geschlossener Label-Raum); Golden-Schema + Draft-Builder + Prelabel (Haiku, OOV-Drop) + Adjudikations-Worksheet + per-Achsen-Metriken + Runner + **Release-Gates-Tabelle** (AC-5).
- **108/108 grün.** Leakage-Caveat ehrlich vermerkt (LLM-Prelabel = dieselbe Modellklasse wie im Eval → im Gate als zweiter unabhängiger Split adressiert).

## Teil 5 — Korpus-Observability + UC-LAW-002-Spec

- **THE-468 (PR #53):** `GET /corpus/status` — Mongo↔Qdrant per-Source-Drift sichtbar.
- **THE-470:** `corpusHealth` wartet den Lazy-Connection-Handshake aus → **kein First-Poll-Fehlalarm** nach Container-Recreate.
- **UC-LAW-002-Spec (`544ae71`):** korpus-weite Discovery (hybrid) spezifiziert + Linear-Traceability-Backfill. Blockiert durch THE-422 (jetzt Done → entblockt).

## PRs & Tickets

| PR | Ticket | Inhalt | Status |
|---|---|---|---|
| [#51](https://github.com/MaCeeeee/TheArchitect/pull/51) | THE-440 | Korpus-Cutover Repo-Seite (DORA live in Prod) | ✅ merged |
| [#52](https://github.com/MaCeeeee/TheArchitect/pull/52) | THE-446 | PROBMGMT Slice 2 — Sentry-Dedup + Fingerprint | ✅ merged |
| [#53](https://github.com/MaCeeeee/TheArchitect/pull/53) | THE-468 | `/corpus/status` Mongo↔Qdrant-Drift | ✅ merged |
| [#54](https://github.com/MaCeeeee/TheArchitect/pull/54) | THE-422 | Governed Retrieval (Read-Side-Gate) | ✅ merged |
| [#55](https://github.com/MaCeeeee/TheArchitect/pull/55) | THE-430/431 | UC-ONTO-001 Slice 1 (C_score + Typing-Eval) | ✅ merged |
| [#56](https://github.com/MaCeeeee/TheArchitect/pull/56) | THE-447/448 | PROBMGMT Slices 3+4 — Closed Loop + LLM + Notify | ✅ merged |
| — | THE-466/467, THE-470 | DORA-Embed-Gap + corpusHealth-Fehlalarm | ✅ auf master |

## 🔜 Morgen hier weitermachen (die offenen Seams)

**Working-Tree-Brotkrumen** (untracked, auf Branch `mganzmanninfo/the-422-…`, inhaltlich = origin/master):
- `scripts/spike-rag-metadata.mjs` — **der Chunk-0-Spike für THE-422**: verifizieren, ob der Data-Server `versionHash` im `chunk.metadata` zurückgibt (+ Homogenität). Das ist der bewusst offengelassene operative Nachweis aus PR #54.
- `reports/uc-scoring-backfill.csv` — Backlog-Scoring-Backfill (Zwischenstand).

**Klar priorisierte nächste Elemente je Initiative:**
1. **CTXGOV / ContextNest (aktuelle Branch, wahrscheinlichster Einstieg):** Chunk-0-Spike laufen lassen → dann die nächsten Slices **THE-423** (ContextTrace), **THE-424/425** (Hash-Chain/Checkpoint), **THE-426** (draft/published-Lifecycle). THE-422 entblockt außerdem **THE-461** (UC-LAW-002 Discovery).
2. **Operational-Governance-Engine:** Engine feature-komplett → jetzt **THE-449** (RISK-Linse erbt die Engine) bauen; den Sentry-n8n-Workflow **aktivieren** (steht validiert-aber-inaktiv).
3. **UC-ONTO-001:** Prelabel-Lauf + menschliche Adjudikation → DSGVO+NIS2-Golden **einfrieren** → `typing:eval` → Baseline gegen die Release-Gates → **THE-432** default-on-Entscheid. Dann THE-430 **Slice 2** (Relations-Eval).
4. **Korpus:** `CORPUS_STRICT_READS` scharfschalten (Fallback aus) — der bewusste THE-440-Folgeschritt, erst nach Beobachtungsfenster (corpusMiss=0).

## Offene Fäden (Altlasten)

- [ ] **Parallel-Session-Git-Race real eingetreten:** die aktuelle Branch `mganzmanninfo/the-422-…` hängt hinter origin/master (PR #55/#56 fehlen im Pointer), Working-Tree-Inhalt ist aber identisch. Vor Weiterarbeit sauber auf origin/master syncen ([[feedback_parallel_session_git_race]]).
- [ ] **Dailies-Backfill:** 07-09 fehlt weiter (leichter Tag, THE-437).
- [ ] **`.superpowers/`** in `.gitignore`.

## Verwandt

[[progress_the440_corpus_cutover]] · [[progress_uc_probmgmt_001]] · [[seed_automated_risk_management]] · [[progress_uc_ctxgov_001]] · [[progress_uc_onto_001]] · [[progress_uc_canon_001]] · [[feedback_asilomar_ai_principles]] · [[feedback_parallel_session_git_race]] · [[feedback_backlog_rescore_trigger]] · THE-440 · THE-446 · THE-447 · THE-448 · THE-422 · THE-430 · THE-431 · THE-461 · THE-449 · THE-432 · UC-LAW-002
